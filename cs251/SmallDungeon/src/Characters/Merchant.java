package Characters;

import Util.Thing;

//Merchant should have a random item for sale, the hero will always attempt to buy it, but might not have enough gold
public class Merchant extends Thing {

    public Merchant(String name, String description) {
        super(name,description);
    }

}
